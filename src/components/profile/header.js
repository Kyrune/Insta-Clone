import React, { useState, UseEffect } from 'react';

export default function Header({
    photosCount,
    followerCount,
    setFollowerCount,
    profile,
    username
}) {
    return (
        <p>Hello</p>
    )
}